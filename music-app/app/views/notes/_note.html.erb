<h4><%= note.user.email %> wrote:</h4>
<%= note.text %>
<% if current_user.id == note.user_id %>
  <%= button_to "Edit note", edit_note_url(note.id), method: :get %>
  <%= button_to "Delete note", note_url(note.id), method: :delete %>
<% end %>